# Settings for the script FGL_selection.py

# detailsSCL: String path to the original CSV file with with all SCL descriptors (e.g. filtering_2005_descriptors.csv),as generated by the Data preparation part of PN Relabeler
detailsSCL: '...\filtering_2005_descriptors.csv'
# label_freq: String path to the CSV file with statistics about each FGL as generated by the Data preparation part of PN Relabeler (e.g. label_freq.csv)
label_freq: '...\label_freq.csv'
# dataset_path: a path where to save the selected FGLs for the development of the PN Relabeling dataset.
dataset_path: '...\FN_2005_full'